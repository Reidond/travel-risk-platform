# Cloudflare Workers static-assets headers (Vite copies this into dist/).
# Hashed build assets are immutable — mirrors frontend/nginx.conf.
/assets/*
  Cache-Control: public, max-age=31536000, immutable
